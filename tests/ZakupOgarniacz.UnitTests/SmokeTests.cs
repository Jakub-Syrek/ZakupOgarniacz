namespace ZakupOgarniacz.UnitTests;

/// <summary>
/// Tymczasowy smoke-test potwierdzający, że projekt testów jednostkowych
/// kompiluje się i jest wykrywany przez runner. Zastąp właściwymi testami
/// logiki z <c>ZakupOgarniacz.Core</c> w kroku 1+ roadmapy.
/// </summary>
public class SmokeTests
{
    [Fact]
    public void Solucja_sie_kompiluje()
    {
        Assert.True(true);
    }
}
